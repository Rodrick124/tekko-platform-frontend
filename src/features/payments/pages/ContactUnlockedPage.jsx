import { ArrowLeft, ShieldAlert } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import mockProviders from '../../../data/mockProviders.js'
import { ROUTES } from '../../../constants/routes.js'
function ContactUnlockedPage(){const {providerId}=useParams();const provider=mockProviders.find((item)=>item.id===providerId);return <section className="mx-auto max-w-2xl px-4 py-20 text-center"><span className="mx-auto grid size-16 place-items-center rounded-full bg-blue-100 text-blue-700"><ShieldAlert size={30}/></span><h1 className="mt-5 text-3xl font-bold">Autorisation à vérifier</h1><p className="mt-3 leading-7 text-slate-600">Le contact ne peut être affiché qu’après vérification d’un droit actif par le backend. Aucun numéro n’est disponible dans les données publiques.</p><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Link to={provider?`/prestataires/${provider.id}`:ROUTES.providers} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 font-bold text-white"><ArrowLeft size={18}/>Retour au prestataire</Link><Link to={ROUTES.clientPayments} className="rounded-xl border border-slate-300 bg-white px-5 py-3 font-bold">Historique des paiements</Link></div></section>}
export default ContactUnlockedPage
