import { BadgeCheck, Clock3, LockKeyhole, MapPin, MessageCircle } from 'lucide-react'
import technicianImage from '../../../assets/images/servicelink-technician.jpg'
import formatCurrency from '../../../utils/formatCurrency.js'
function PaymentSummary({ provider, amount = 100 }) { return <aside className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex items-start justify-between gap-3"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">Prestataire sélectionné</p><span className="rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-bold text-emerald-700">Vérifié</span></div><div className="mt-4 flex gap-3"><img src={technicianImage} alt={`Portrait de ${provider.name}`} className="size-16 rounded-xl object-cover"/><div><h2 className="font-bold">{provider.name}</h2><p className="text-xs text-slate-500">{provider.category}</p><p className="mt-1 flex items-center gap-1 text-[10px] text-slate-500"><MapPin size={12}/>{provider.city}</p></div></div><div className="mt-5 space-y-3 rounded-xl bg-blue-50 p-4 text-xs text-slate-700"><p className="flex gap-2"><LockKeyhole size={16} className="shrink-0 text-blue-700"/>Le numéro WhatsApp reste entièrement caché.</p><p className="flex gap-2"><Clock3 size={16} className="shrink-0 text-blue-700"/>Accès personnel valable 24 heures après confirmation serveur.</p><p className="flex gap-2"><MessageCircle size={16} className="shrink-0 text-blue-700"/>Ouverture de WhatsApp pour échanger directement.</p></div><div className="mt-5 flex items-end justify-between rounded-xl bg-blue-100 p-4"><span><small className="block font-semibold text-slate-500">Montant total</small><small className="flex items-center gap-1 text-slate-500"><BadgeCheck size={12}/>Sans frais cachés</small></span><strong className="text-3xl text-blue-700">{formatCurrency(amount)}</strong></div></aside> }
export default PaymentSummary
