import { ChevronDown, RotateCcw, SlidersHorizontal } from 'lucide-react'
import { useState } from 'react'
import mockCategories from '../../../data/mockCategories.js'
function ProviderFilters({ filters, onChange, onReset }) { const [open,setOpen]=useState(false); const fields=<><label className="grid gap-1 text-xs font-semibold">Catégorie<select value={filters.category} onChange={(event)=>onChange('categorie',event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2.5 font-normal"><option value="">Toutes les catégories</option>{mockCategories.map((category)=><option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="grid gap-1 text-xs font-semibold">Disponibilité<select value={filters.availability} onChange={(event)=>onChange('disponibilite',event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2.5 font-normal"><option value="">Toutes</option><option value="maintenant">Disponible maintenant</option><option value="plus-tard">Disponible plus tard</option></select></label><label className="grid gap-1 text-xs font-semibold">Trier par<select value={filters.sort} onChange={(event)=>onChange('tri',event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2.5 font-normal"><option value="pertinence">Pertinence</option><option value="nom">Nom</option><option value="evaluation">Évaluation</option></select></label></>; return <div><button type="button" className="flex w-full items-center justify-between rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-bold md:hidden" aria-expanded={open} aria-controls="secondary-filters" onClick={()=>setOpen((value)=>!value)}><span className="flex items-center gap-2"><SlidersHorizontal size={18}/>Filtres</span><ChevronDown size={18} className={open?'rotate-180 transition-transform':'transition-transform'}/></button><div id="secondary-filters" className={`${open?'grid':'hidden'} mt-3 gap-3 md:grid md:grid-cols-[1fr_1fr_1fr_auto] md:items-end`} >{fields}<button type="button" onClick={onReset} className="inline-flex items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-xs font-bold hover:bg-slate-50"><RotateCcw size={15}/>Réinitialiser</button></div></div> }
export default ProviderFilters
