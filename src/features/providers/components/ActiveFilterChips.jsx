import { X } from 'lucide-react'
function ActiveFilterChips({items,onRemove,onReset}){if(!items.length)return null;return <div className="flex flex-wrap items-center gap-2" aria-label="Filtres actifs"><span className="text-[10px] font-bold uppercase text-slate-500">Filtres actifs :</span>{items.map((item)=><button type="button" key={item.key} onClick={()=>onRemove(item.key)} className="inline-flex items-center gap-1 rounded-full bg-blue-100 px-3 py-1.5 text-[10px] font-bold text-blue-800 hover:bg-blue-200">{item.label}<X size={12}/><span className="sr-only">Supprimer ce filtre</span></button>)}<button type="button" onClick={onReset} className="text-[10px] font-bold text-blue-700 underline underline-offset-2">Effacer tous les filtres</button></div>}
export default ActiveFilterChips
