import { Clock3, LockKeyhole, MapPin, MessageCircle, Star } from 'lucide-react'
import { Link } from 'react-router-dom'
import technicianImage from '../../../assets/images/servicelink-technician.jpg'
function ProviderCard({ provider }) { return <article className="flex flex-col rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200 transition hover:-translate-y-1 hover:shadow-lg"><div className="flex gap-3"><div className="relative"><img src={technicianImage} alt={`Portrait de ${provider.name}`} className="size-16 rounded-xl object-cover"/><span className={`absolute -bottom-1 -right-1 size-3.5 rounded-full ring-2 ring-white ${provider.available?'bg-green-500':'bg-amber-400'}`}/></div><div className="min-w-0"><h2 className="truncate font-bold">{provider.name}</h2><p className="text-xs text-slate-600">{provider.category}</p><p className={`mt-1 text-[10px] font-bold ${provider.available?'text-emerald-700':'text-amber-700'}`}>{provider.available?'● Disponible':'● Disponible plus tard'}</p></div></div><div className="mt-4 flex flex-wrap gap-x-3 gap-y-2 text-[10px] text-slate-500"><span className="inline-flex items-center gap-1"><MapPin size={12}/>{provider.district}, {provider.city}</span><span className="inline-flex items-center gap-1 text-amber-700"><Star size={12}/>{provider.rating} ({provider.jobs} interventions)</span></div><p className="mt-4 text-xs leading-5 text-slate-600">{provider.description}</p><div className="mt-3 flex flex-wrap gap-2">{provider.services.map((service)=><span key={service} className="rounded bg-blue-50 px-2 py-1 text-[10px] text-blue-800">{service}</span>)}</div><p className="mt-4 flex items-center gap-2 rounded-lg bg-blue-50 px-3 py-2 text-[10px] font-semibold text-slate-600"><Clock3 size={13} className="text-emerald-700"/>Arrivée estimée : {provider.eta}</p><p className="mt-2 flex items-center gap-2 rounded-lg bg-slate-50 px-3 py-2 text-[10px] text-slate-500"><LockKeyhole size={13}/>Contact protégé — aucune coordonnée publique</p><div className="mt-4 grid grid-cols-[auto_1fr] gap-2"><Link to={`/prestataires/${provider.id}`} className="rounded-lg bg-blue-50 px-4 py-2.5 text-center text-xs font-bold text-blue-800 hover:bg-blue-100">Profil</Link><Link to={`/paiement/${provider.id}`} className="inline-flex items-center justify-center gap-2 rounded-lg bg-[#006056] px-3 py-2.5 text-xs font-bold text-white hover:bg-[#007b6e]"><MessageCircle size={15}/>WhatsApp <span className="rounded bg-black/20 px-1.5 py-0.5">100 FCFA</span></Link></div></article> }
export default ProviderCard
