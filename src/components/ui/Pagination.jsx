import { ChevronLeft, ChevronRight } from 'lucide-react'
function Pagination({ page=1,totalPages=1,onChange=()=>{} }) { const pages=Array.from({length:totalPages},(_,index)=>index+1); return <nav aria-label="Pagination" className="flex flex-wrap items-center justify-center gap-2"><button type="button" disabled={page<=1} onClick={()=>onChange(page-1)} className="inline-flex items-center gap-1 rounded-lg border bg-white px-3 py-2 text-xs disabled:cursor-not-allowed disabled:opacity-40"><ChevronLeft size={14}/>Précédent</button>{pages.map((number)=><button type="button" key={number} aria-current={number===page?'page':undefined} onClick={()=>onChange(number)} className={`size-9 rounded-lg text-xs font-bold ${number===page?'bg-blue-600 text-white':'border bg-white hover:bg-blue-50'}`}>{number}</button>)}<button type="button" disabled={page>=totalPages} onClick={()=>onChange(page+1)} className="inline-flex items-center gap-1 rounded-lg border bg-white px-3 py-2 text-xs disabled:cursor-not-allowed disabled:opacity-40">Suivant<ChevronRight size={14}/></button></nav> }
export default Pagination
